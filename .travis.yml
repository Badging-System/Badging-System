language: node_js
node_js:
  - "10"
os:
  - linux
  - osx
  - windows
install:
  - npm install -g
before_deploy:
  - eval "$(ssh-agent -s)"
  - ssh-agent -k
script:
  - npm start -verbose
  - ps
services:
  - mongodb
notifications:
    slack: $SLACK
    if: branch = Task-#52
    on_success: always
    on_failure: false
    on_pull_requests: false
    email: false
env:
  - ENV=$ENVIRONMENT PORT=$PORT HOST=$HOST DBNAME=$DBNAME
